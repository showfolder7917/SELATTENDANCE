<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sp.selfsp.uniauth.tenant.dao.UniauthTenantDao">

  <resultMap id="TenantItemResultMap" type="com.sp.selfsp.uniauth.tenant.domain.out.UniauthTenantItemOut">
    <id column="id" property="id"/>
    <result column="tenant_code" property="tenantCode"/>
    <result column="tenant_name" property="tenantName"/>
    <result column="tenant_status" property="tenantStatus"/>
    <result column="contact_name" property="contactName"/>
    <result column="contact_email" property="contactEmail"/>
    <result column="contact_phone" property="contactPhone"/>
    <result column="created_at" property="createdAt"/>
    <result column="updated_at" property="updatedAt"/>
  </resultMap>

  <sql id="TenantItemColumns">
    id, tenant_code, tenant_name, tenant_status, contact_name, contact_email, contact_phone, created_at, updated_at
  </sql>

  <insert id="insertTenant">
    INSERT INTO ua_tenant (
      tenant_code, tenant_name, tenant_status, contact_name, contact_email, contact_phone
    ) VALUES (
      #{in.tenantCode}, #{in.tenantName}, #{in.tenantStatus}, #{in.contactName}, #{in.contactEmail}, #{in.contactPhone}
    )
  </insert>

  <update id="updateTenant">
    UPDATE ua_tenant
    SET tenant_code = #{in.tenantCode},
        tenant_name = #{in.tenantName},
        tenant_status = #{in.tenantStatus},
        contact_name = #{in.contactName},
        contact_email = #{in.contactEmail},
        contact_phone = #{in.contactPhone},
        updated_at = CURRENT_TIMESTAMP
    WHERE id = #{in.id}
  </update>

  <select id="selectTenantByCode" resultMap="TenantItemResultMap">
    SELECT <include refid="TenantItemColumns"/>
    FROM ua_tenant
    WHERE tenant_code = #{tenantCode}
  </select>
</mapper>
