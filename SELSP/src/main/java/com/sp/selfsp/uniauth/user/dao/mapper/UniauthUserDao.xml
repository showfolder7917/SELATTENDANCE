<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sp.selfsp.uniauth.user.dao.UniauthUserDao">

  <resultMap id="UserItemResultMap" type="com.sp.selfsp.uniauth.user.domain.out.UniauthUserItemOut">
    <id column="id" property="id"/>
    <result column="tenant_id" property="tenantId"/>
    <result column="tenant_code" property="tenantCode"/>
    <result column="login_name" property="loginName"/>
    <result column="display_name" property="displayName"/>
    <result column="display_name_kana" property="displayNameKana"/>
    <result column="locale" property="locale"/>
    <result column="email" property="email"/>
    <result column="phone" property="phone"/>
    <result column="user_status" property="userStatus"/>
    <result column="locked_flag" property="lockedFlag"/>
  </resultMap>

  <sql id="UserItemColumns">
    u.id, u.tenant_id, t.tenant_code, u.login_name, u.display_name, u.display_name_kana, u.locale, u.email, u.phone, u.user_status, u.locked_flag
  </sql>

  <insert id="insertUser">
    INSERT INTO ua_user (
      tenant_id, login_name, password_hash, display_name, display_name_kana, locale, email, phone, user_status, locked_flag
    ) VALUES (
      #{in.tenantId}, #{in.loginName}, #{passwordHash}, #{in.displayName}, #{in.displayNameKana}, #{in.locale}, #{in.email}, #{in.phone}, #{in.userStatus}, 0
    )
  </insert>

  <update id="updateUser">
    UPDATE ua_user
    SET tenant_id = #{in.tenantId},
        login_name = #{in.loginName},
        password_hash = CASE WHEN #{passwordHash} = '' THEN password_hash ELSE #{passwordHash} END,
        display_name = #{in.displayName},
        display_name_kana = #{in.displayNameKana},
        locale = #{in.locale},
        email = #{in.email},
        phone = #{in.phone},
        user_status = #{in.userStatus},
        updated_at = CURRENT_TIMESTAMP
    WHERE id = #{in.id}
  </update>

  <select id="selectUserByLoginName" resultMap="UserItemResultMap">
    SELECT <include refid="UserItemColumns"/>
    FROM ua_user u
    LEFT JOIN ua_tenant t ON t.id = u.tenant_id
    WHERE u.login_name = #{loginName}
  </select>

  <select id="selectUserById" resultMap="UserItemResultMap">
    SELECT <include refid="UserItemColumns"/>
    FROM ua_user u
    LEFT JOIN ua_tenant t ON t.id = u.tenant_id
    WHERE u.id = #{id}
  </select>

  <delete id="deleteUserRoles">
    DELETE FROM ua_user_role WHERE user_id = #{userId}
  </delete>

  <insert id="insertUserRole">
    INSERT INTO ua_user_role (user_id, role_id) VALUES (#{userId}, #{roleId})
  </insert>
</mapper>
